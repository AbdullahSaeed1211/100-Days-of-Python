# Raising Custom Errors
a=int(input("enter any value between 5 and 9"))
if (a<5 or a>9):
    raise ValueError{"Value should be between 5 & 9"}
# We can rause custom errors using raise 
# defining custom exceptions using class
